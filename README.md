This is a repo containing a from-scratch implementation of `micrograd`, specifically containing a `Value` class and a neural network implementation.

The `training_XORnet.py` contains the training (via grad-descent) and testing loop (data is generated randomly) to train the MLP to learn the XOR function
